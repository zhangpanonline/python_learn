my_list = ['a', 'b', 'c', 'd', 'e']

# enumerate() 函数用于将一个可遍历的数据对象（如列表、元组或字符串）组合为一个索引序列，同时列出数据和数据下标，一般用在for循环当中
# eg:
# for j in enumerate(my_list):
#     print(j, type(j))
# (1, 'b') <class 'tuple'>
# (2, 'c') <class 'tuple'>
# (3, 'd') <class 'tuple'>
# (4, 'e') <class 'tuple'>


# 公共方法：

# + 支持字符串、列表、元组进行操作，得到一个新的容器
# * 乘整数，进行复制，支持字符串、列表、元组进行操作，得到一个新的容器
# in/not in 判断存在或者是不存在，支持字符串、列表、元组、字典进行操作，如果是字典，判断的是字典的key是否存在

# del
# len()
# max()
# min()
